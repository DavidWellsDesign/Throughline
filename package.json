{
  "name": "throughline-site",
  "private": true,
  "version": "0.1.0",
  "description": "Landing page and Stripe fulfilment for Throughline Progression and Throughline Balance.",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8000 --directory public",
    "test": "node scripts/test-webhook.mjs",
    "shots": "node scripts/shot-progression.mjs && node scripts/shot-balance.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^25.10.0",
    "wrangler": "^4.129.0"
  }
}
